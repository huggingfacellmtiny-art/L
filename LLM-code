from transformers import pipeline

llm = pipeline("text-generation", model="gpt2")

prompt = "Artificial Intelligence is"
response = llm(prompt, max_new_tokens=30)

print(response[0]["generated_text"])
